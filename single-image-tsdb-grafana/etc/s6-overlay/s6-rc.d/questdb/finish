#!/bin/bash
# This script runs when the questdb service exits
# Exit code 0 means normal shutdown, non-zero means error
if [ $1 -eq 0 ]; then
    echo "QuestDB exited normally"
    exit 0
else
    echo "QuestDB exited with error code $1"
    exit 1
fi
